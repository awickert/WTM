# Generated scaling artifacts -- do NOT track. The parent benchmark/.gitignore
# negates *.tif/*.cfg to keep the committed benchmark inputs; re-ignore the
# synthetic grids (potentially many GB) and sweep outputs here.
synth/
*.csv
results.csv
__pycache__/
fail_*.log

# ...but DO track curated, dated result sets meant for publication.
!results_2026-*.csv
